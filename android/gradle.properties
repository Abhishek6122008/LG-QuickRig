# Modest heap so the build can run alongside the emulator on a 16GB machine —
# the previous 8G+4G setting OOM'd the Kotlin compiler (paging file exhausted).
org.gradle.jvmargs=-Xmx2G -XX:MaxMetaspaceSize=768m -XX:+HeapDumpOnOutOfMemoryError
kotlin.daemon.jvmargs=-Xmx1G
android.useAndroidX=true
